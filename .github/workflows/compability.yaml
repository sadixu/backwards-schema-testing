
name: Validate Backwards Compability GraphQL Schema 
on: [push]
jobs:
  # test:
  #   name: Check Schema
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@main
  #     - name: Check Address Schema
  #       uses: kamilkisiela/graphql-inspector@master
  #       with:
  #         schema: 'main:schemas/address.graphql'
  #     - name: Check Person Schema
  #       uses: kamilkisiela/graphql-inspector@master
  #       with:
  #         schema: 'main:schemas/person.graphql'
  test2:
    name: Check Merged Schema
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
        with:
          path: main
      - name: Merge main schemas
        run: |
          cd main/schemas
          cat address.graphql person.graphql > schema.graphql
          cat schema.graphql
      - name: Checkout feature branch
        uses: actions/checkout@v2
        with:
          path: feature
      - name: Merge feature branch schemas
        run: |
          cd feature/schemas
          cat address.graphql person.graphql > schema.graphql
          cat schema.graphql
      - name: Compare merged schemas
        run: |
          ls
      - name: Check Address Schema
        uses: kamilkisiela/graphql-inspector@master
        with:
          schema: 'main:schemas/address.graphql'
      - name: Check Person Schema
        uses: kamilkisiela/graphql-inspector@master
        with:
          schema: 'main:schemas/person.graphql'